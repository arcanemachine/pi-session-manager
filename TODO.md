# TODO

- Resolve ownership of the already-registered unscoped npm name
  `pi-session-manager` before npm publication, or make a separate explicit
  package-identity decision. Git and local installation remain supported.
